# sub-make to define pre-macros.
# 
# usage:
#	include make_p
# before SUB_PATH && PRE_MACRO to 
# include make_p. CTX_PATH only 
# used by src/context/Makefile.
#
# lxr, 2020.02

ifeq ($(CCTX), CCTX_ASM)
	CTX_PATH  += assembly
	PRE_MACRO += -DCCTX_ASM
else ifeq ($(CCTX), CCTX_UC)
	CTX_PATH  += ucontext
	PRE_MACRO += -DCCTX_UC
endif
